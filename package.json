{
  "name": "smartie",
  "version": "0.1.0",
  "description": "A smart screen recorder for polished demo videos with smooth auto zoom and feature toggles.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron --no-sandbox .",
    "smoke": "electron --no-sandbox . --smoke-test",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node --check src/native-telemetry.js && node --check scripts/doctor.mjs && node --check scripts/eval-director.mjs && node --check scripts/eval-telemetry.mjs && node scripts/smoke-check.mjs",
    "eval:director": "node scripts/eval-director.mjs",
    "eval:telemetry": "node scripts/eval-telemetry.mjs",
    "doctor": "node scripts/doctor.mjs",
    "package:linux": "electron-packager . Smartie --platform=linux --arch=x64 --out=dist --overwrite --prune=true --asar.unpack='**/node_modules/ffmpeg-static/**' --ignore='^/dist($|/)' --ignore='^/node_modules/.cache($|/)'"
  },
  "keywords": [
    "screen-recorder",
    "electron",
    "demo",
    "smart-zoom",
    "smartie"
  ],
  "author": "AKMessi",
  "license": "MIT",
  "devDependencies": {
    "@electron/packager": "^20.0.0",
    "electron": "^42.2.0"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0"
  }
}
